package leetcode


//给定一个整数n 和数组A，返回A中数字组成的比n小的最大数字，例n = 2333，A[2，3，4]，结果2324。
fun getMax(target: Int, nums: Array<Int>) {
    val s = target.toString()
    for (c in s) {
        for (num in nums) {

        }
    }
}

fun main() {
    getMax(2324, arrayOf(2,3,4))
}